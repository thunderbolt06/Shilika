# Shilika site — Next.js + Supabase setup

End-to-end setup for the migrated site, the admin control plane, and the
autonomous content pipeline. Five phases, all live now.

## 1. Rotate the leaked Supabase secret first

The `sb_secret_…` key that was pasted in chat must be rotated **before any deploy**.

- Open the Supabase dashboard for project `yzgaxyxoosazxfnsyqta`
- Project Settings → API → click **Roll** on the `service_role` (secret) key
- Copy the new secret into `.env.local` (see step 3) and into Vercel env vars (step 8)
- Do **not** commit `.env.local`

## 2. Install dependencies

```bash
npm install
```

## 3. Local environment

```bash
cp .env.example .env.local
```

Required (Phase 1 + 3 — the admin and blog read paths):
- `NEXT_PUBLIC_SITE_URL` — `http://localhost:3000` locally, `https://www.shilikajain.com` on prod
- `NEXT_PUBLIC_SUPABASE_URL` — `https://yzgaxyxoosazxfnsyqta.supabase.co`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` — the `sb_publishable_…` key (client-safe)
- `SUPABASE_SECRET_KEY` — the **rotated** `sb_secret_…` key (server-only)
- `ADMIN_PASSWORD` — 12+ chars, your choice
- `ADMIN_SESSION_SECRET` — 32+ chars, random (used to HMAC-sign session cookies)

Required for Phase 4 (writer + image gen):
- `ANTHROPIC_API_KEY` — claude-opus-4-7 for the writer agent
- `GOOGLE_GENAI_API_KEY` — Gemini "Nano Banana 2" / Imagen 3 for hero images
- Optional: `ANTHROPIC_WRITER_MODEL`, `ANTHROPIC_RESEARCH_MODEL` (defaults to `claude-opus-4-7`)
- Optional: `IMAGE_GEN_MODEL` (defaults to `gemini-2.5-flash-image-preview`)
- Optional: `IMAGE_GEN_PROVIDER=stub` to short-circuit the API and return a placeholder

Required for Phase 5 (cron + analytics + cross-post):
- `CRON_SECRET` — 32+ chars random. Vercel Cron sends this in `Authorization: Bearer …`
- `GSC_SITE_URL=sc-domain:shilikajain.com`
- `GSC_SERVICE_ACCOUNT_JSON` — full service account JSON, one line, escaped for env
- `GA_PROPERTY_ID=123456789`
- `GA_SERVICE_ACCOUNT_JSON` — same shape as GSC
- Optional cross-post: `DEVTO_API_KEY`, `HASHNODE_API_KEY`, `HASHNODE_PUBLICATION_ID`, `MEDIUM_INTEGRATION_TOKEN`, `MEDIUM_USER_ID`

## 4. Apply database migrations

```
supabase/migrations/
  0001_blog_foundation.sql      blog_posts, content_ideas, authors, knowledge_base, analytics, content_metrics
  0002_storage_bucket.sql       media bucket
  0003_seed_author.sql          Shilika as the default author
  0004_seed_posts.sql           3 SEO-optimised playbook seed posts
  0005_cron_runs.sql            cron run ledger
```

Two ways to apply:

**Option A — Supabase Dashboard SQL editor (fastest one-time):** open each file, paste, run, in order.

**Option B — Supabase CLI:**
```bash
npm i -g supabase
supabase link --project-ref yzgaxyxoosazxfnsyqta
supabase db push
```

## 5. Local dev

```bash
npm run dev   # http://localhost:3000
```

| URL | Purpose |
|---|---|
| `/` | Editorial homepage (lift-and-shift from legacy index.html) |
| `/blog`, `/blog/[slug]` | Public blog index + post renderer |
| `/api/markdown/blog/[slug]` | AI-bot-friendly markdown alternate with canonical Link |
| `/sitemap.xml`, `/robots.txt` | Generated by Next.js |
| `/about`, `/services/*`, `/work/*`, `/article-*` | Native Next.js routes (Phase 2 port) |
| `/admin/login` | Password gate |
| `/admin` | Overview + live stats |
| `/admin/blog/review`, `/admin/blog/review/[id]` | Draft review queue + editor |
| `/admin/content-ideas` | Topic backlog. Run writer per row. |
| `/admin/blog/posts` | Catalog. Publish / unpublish / regenerate / delete. |
| `/admin/blog/excel` | Bulk upsert + export by id |
| `/admin/knowledge-base` | Case studies + brand voice + long-tail seeds |
| `/admin/cron` | Last-run table + manual trigger buttons |

## 6. Smoke test the writer

1. Add a long-tail seed via `/admin/knowledge-base`. Pick `long_tail_seed` as kind, paste 5-10
   queries one per line into the body field.
2. Open `/admin/cron`, hit "Run now" on `queue-filler`. Claude proposes ~5 ideas into
   `content_ideas`.
3. Open `/admin/content-ideas`, pick the top P0 / P1 row, hit "Run writer". The writer drafts the
   post, runs the humanization validator until clean, generates a hero image, and flips the row to
   `ready_for_review`.
4. Open `/admin/blog/review/[id]`, scan the post, optionally edit, hit "Save & approve".
5. Open `/admin/cron`, hit "Run now" on `hourly-publisher`. The row promotes to `blog_posts` and
   shows up on `/blog`.

## 7. Cron triggers — how it works in production

Vercel Cron config lives in `vercel.json` under the `crons` array. Vercel invokes each path on the
schedule with `Authorization: Bearer ${CRON_SECRET}`.

| Cron | Schedule (UTC) | What it does | Requires |
|---|---|---|---|
| `gsc-daily-pull` | 04:00 daily | 28-day Search Console rollup → `analytics_search_console` | GSC env vars |
| `ga-daily-pull` | 04:30 daily | 28-day GA4 pagePath × date metrics → `content_metrics` | GA env vars |
| `queue-filler` | 05:00 daily | Topic-research agent. Reads GSC opportunities + long-tail seeds. Proposes 5-10 ideas. | ANTHROPIC_API_KEY (+ GSC optional) |
| `daily-writer` | 05:30 daily | Picks the top P0/P1 idea, runs writer + image gen, marks `ready_for_review` | ANTHROPIC_API_KEY (+ GOOGLE_GENAI_API_KEY optional) |
| `hourly-publisher` | every hour | Picks approved ideas, upserts blog_posts, revalidates paths | — |
| `cross-poster` | :30 every hour | Republishes new posts to dev.to / Hashnode / Medium with canonical Link | Platform tokens optional |
| `seo-content-analysis` | Sun 06:00 | Weekly brief joining GSC + GA, saved as a knowledge_base row the writer reads | — |

The 30-minute offsets (filler 05:00, writer 05:30; publisher :00, cross-poster :30) are deliberate
— a job must complete before its downstream picks up the result.

**Manual trigger from the UI:** `/admin/cron` → "Run now" on any row. The button hits
`/api/admin/cron/trigger/{name}` which proxies to the real cron route with the secret. Both
paths produce a real `cron_runs` ledger entry.

**Manual trigger via curl:**
```bash
curl -H "Authorization: Bearer $CRON_SECRET" \
  https://www.shilikajain.com/api/cron/queue-filler?force=1
```

**Monitoring:** `/admin/cron` shows last-run timestamp + status (success / partial / error) +
summary JSON per cron. A failed cron also writes its error string. For real alerting:
- Vercel Dashboard → Functions → filter by `cron-` prefix for invocation logs
- Add Supabase RPC + a webhook to a Slack channel on `status='error'` rows (Phase 6 if needed)

**What if a cron misses a tick:** Vercel Cron retries failed invocations once after 30s. Beyond
that, the next scheduled run picks up the work because every job is idempotent:
- `queue-filler` skips if the queue has ≥5 ideas
- `daily-writer` picks the highest-priority idea, regardless of which one was missed
- `hourly-publisher` drains every approved row
- `cross-poster` marks posts as cross-posted in `content_ideas.source_signals` so reruns skip them
- `gsc-daily-pull` and `ga-daily-pull` upsert by composite keys (idempotent on retry)

## 8. Vercel project setup

In Vercel → Project Settings → Environment Variables, add the env vars from step 3. Mark each as
**Production** + **Preview** + **Development** unless you want preview to point at a different DB.

Vercel auto-detects Next.js. The `crons` array in `vercel.json` is picked up at deploy time.

**Plan requirements:** Vercel Cron with maxDuration > 60s requires the Pro plan or Fluid Compute.
The daily-writer and queue-filler routes declare `maxDuration = 300` so they need 5-min runtime —
verify the project plan supports it before deploy.

## 9. Storage bucket

The `media` Supabase Storage bucket holds blog hero images (and any other media you upload
through the admin). Public read, service-role write. Created by `0002_storage_bucket.sql`.

## 10. Where the agent's brain lives

```
agents/content-strategist/
  PERSONA.md          identity, voice, what you do/do not invent
  STRATEGY.md         4Ps positioning, audience, channels
  IMPORTANT.md        running memory across runs (append when something surprising happens)

skills/write-blog-post/SKILL.md     output envelope + 10x quality bar

docs/
  humanization-guide.md   the rules the validator enforces
  geo-guidelines.md       how to write so AI assistants cite cleanly
```

The writer loads all of these on every run. The topic-research agent loads PERSONA + STRATEGY.
Knowledge base entries (`/admin/knowledge-base`) are layered on top for grounded claims.

## 11. Module layout

```
app/
  (public marketing pages)
  blog/                 public blog
  admin/                gated admin
  api/admin/            gated by middleware
  api/cron/             gated by CRON_SECRET
agents/                 persona + memory + strategy
skills/                 writer skill spec
docs/                   humanization + GEO guides
lib/
  agents/               writer, topic-research, pipeline, context, humanize loop
  images/               Gemini + sharp + Supabase Storage pipeline
  integrations/         google-auth, gsc, ga, cross-post
  supabase/             clients + types
  admin-session.ts      HMAC session cookie (Web Crypto, Edge compatible)
  cron-auth.ts          CRON_SECRET gate + cron_runs ledger
middleware.ts           /admin/* and /api/admin/* gate
scripts/
  check-humanization.mjs   validator CLI + library
  port-pages.mjs           Phase 2 lift-and-shift port script
supabase/migrations/    five files, applied in order
```

# Important — Memory

Running log of lessons learned across drafts. The writer reads this at the start of every run.

Format: one line per lesson. Lead with the rule. Append `Why:` so future runs can judge whether the rule still applies.

Keep this file under ~50 lines. Prune the oldest or least relevant when it grows past that.

---

(none yet — the file fills in as the writer ships drafts and Shilika reviews them)
